name: Hello CI

on:
  push:
    branches:
      - joska
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Say Hello
        run: echo "GitHub Actions is working!"
